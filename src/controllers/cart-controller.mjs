const postCart = async (req, res) => {
  // adds a new item to the order
  // TODO: check if order by the id exists and if yes, check that the order status is correct
  // TODO: check if the user by the id exists
};

export {postCart};
